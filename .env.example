# Local development — copy to .env and fill in
# These are set as SST Secrets in production (sst secret set <Name> <value> --stage prod)
ADMIN_PASSWORD=changeme
JWT_SECRET=change-me-to-a-random-string
